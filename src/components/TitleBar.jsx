import React from 'react'

const TitleBar = ({channelId,title}) => {
    console.log(channelId)
    console.log(title);

  return (
    <div>
        TitleBar
    </div>
  )
}

export default TitleBar